# --------- pytorch & numpy --------- #
torch==2.0.0
torchvision==0.15.1
torchaudio==2.0.1
pytorch-lightning==2.0.0
torchmetrics==0.11.0
numpy==1.23.0

# --------- MIDI related --------- #
pretty-midi==0.2.10
miditoolkit==0.1.16
miditok==1.4.3
jams==0.3.4
librosa==0.9.2
soundfile                # Audio backend for librosa (uses libsndfile)
pysoundfile              # Alias package

# --------- others --------- #
scikit-learn==1.3.2     # pinned for pre-built wheels (avoids GCC >= 8 requirement)
typeguard==2.13.3
espnet==202301
matplotlib==3.7.1       
pyrootutils==1.0.4      # standardizing the project root setup
rich==13.3.2            # beautiful text formatting in terminal
boto3                   # AWS SDK (included in Lambda but adding explicitly)


h5py==3.10.0            # pinned for pre-built wheels
llvmlite==0.41.1        # pinned for pre-built wheels (required by numba)
sentencepiece==0.1.99   # pinned for pre-built wheels